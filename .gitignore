tt.*
.tests
doc/tags
debug
.repro
foo.*
*.log
data
# ~/.config/nvim/.gitignore additions
# Neovim Cache & State
.nvim/
autoload/
view/
shada/
swap/
backup/

# Lazy.nvim/Mason generated files
lazy-lock.json      # Locks plugin versions
.luarc.json         # Lua language server files
.vscode/            # Possibly generated by a plugin

# Plugin managers often install things here (if not using Lazy's default)
plugged/
pack/
